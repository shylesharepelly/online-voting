<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="./css/styles.css" rel="stylesheet">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body style="background-image: linear-gradient(43deg,rgba(157, 255, 242, 0.566),rgba(168, 255, 168, 0.566))">

    <div>
        <%- include('head.ejs') %>
    </div>
    <div>
        <button  name="goback" class="text-white bg-green-600 ml-5 hover:bg-green-800 font-medium rounded px-5 py-2"><a href="/elections" class=" px-4  p-2 font-bold text-xl">Go Back</a></button>
    </div>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <h6 class="py-4 text-4xl font-bold">Create New Election</h6>
            

            <div class="text-red-600 ml-5 font-bold text-2xl">
                
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                            <li> <%= messages.error[i] %></li>
                    <% } %>
                  <% } %>
            </div>

            <form action="/elections/new" method="post">
                <input type="hidden" name="_csrf" value="<%=csrfToken %>" />
 
                <div class="py-3">
                    <label>What is your Election's name?</label>
                    <input type="text" name="title" class="border border-gray-300 text-black-300 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Submit</button>
                </div>
            

            </form>
        </div>
    </div>

</body>
</html>